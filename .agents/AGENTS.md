# Project Rules

- When the user requests a deployment or push (e.g., "배포해줘", "배포해보자", "배포하자", "배포"), execute the `git push` command directly using `run_command` without seeking textual confirmation or creating an implementation plan.
