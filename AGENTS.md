# 부부한의원 홈페이지 AI 작업 지침

이 문서는 Codex, Antigravity 및 기타 AI 코딩 도구가 저장소에서 작업할 때 따라야 할 공통 규칙이다.

## 1. 프로젝트 목적

- 운영 사이트: `https://booboo.co.kr`
- 목적: 부부한의원 홈페이지의 안정성, SEO, AEO, 접근성 및 예약 전환 구조를 단계적으로 개선한다.
- 코드의 단일 진실 공급원은 이 GitHub 저장소의 `main` 브랜치다.
- AI 채팅 기록이나 특정 컴퓨터의 로컬 파일을 최신본으로 간주하지 않는다.

## 2. 작업 시작 전 필수 절차

1. 저장소 루트의 `AGENTS.md`, 기존 `README.md`, 관련 Issue와 Pull Request를 읽는다.
2. 다음 명령으로 최신 상태를 확인한다.

```bash
git fetch origin
git switch main
git pull --ff-only origin main
git status
git log -5 --oneline
```

3. 미커밋 변경사항이 있으면 원인을 확인하기 전까지 작업하지 않는다.
4. 한 번에 하나의 Issue만 처리한다.
5. 최신 `main`에서 작업 전용 브랜치를 만든다.

```bash
git switch -c feat/seo-000-short-description
```

## 3. 브랜치 규칙

- 기능: `feat/seo-번호-설명`
- 수정: `fix/seo-번호-설명`
- 문서: `docs/seo-번호-설명`
- 유지보수: `chore/설명`
- `main`에 직접 커밋하지 않는다.
- 서로 다른 AI 도구가 같은 브랜치나 같은 파일을 동시에 수정하지 않는다.

## 4. 작업 범위

- Issue의 완료 조건을 충족하는 최소 범위만 수정한다.
- 범위 밖 문제를 발견하면 현재 작업에 섞지 말고 별도 Issue 후보로 기록한다.
- 기존 디자인과 사용자 흐름을 불필요하게 바꾸지 않는다.
- 운영 사이트, 배포 설정, 도메인, 리디렉션 변경은 되돌리기 방법을 먼저 확인한다.

## 5. 의료 콘텐츠 및 개인정보 안전 규칙

- 환자 정보, 직원 개인정보, 비밀번호, API 키, 토큰, 쿠키를 코드·커밋·로그에 포함하지 않는다.
- 실제 비밀값은 GitHub Secrets 또는 배포 플랫폼 환경변수에서 관리한다.
- 치료 효과를 보장하거나 즉시성을 암시하는 표현을 임의로 추가하지 않는다.
- 특히 다음 표현은 의료광고 검토 없이 사용하지 않는다: `완치`, `100%`, `보장`, `즉시 효과`, `최고`, `유일`, `부작용 없음`, `근손실 없음`.
- 의료 문구 변경은 사실 확인과 의료진 검수를 전제로 한다.
- 환자 후기를 공개 페이지에 임의로 추가하지 않는다.

## 6. SEO·AEO 기본 규칙

- 공식 URL은 `https://booboo.co.kr/`을 기준으로 하되, 실제 배포 설정과 일치하는지 확인한다.
- 페이지마다 고유한 Title, Meta description, canonical, H1을 사용한다.
- 진료·질환·의료진 콘텐츠는 가능한 한 독립 URL을 사용한다.
- JSON-LD의 병원명, 주소, 전화번호, 진료시간은 화면 표시값과 일치해야 한다.
- 구조화 데이터는 보이지 않는 내용을 과장하거나 새로 만들지 않는다.
- FAQ 스키마는 실제 화면에 표시되는 질문과 답변에만 적용한다.
- `robots.txt`, `sitemap.xml`, canonical, HTTP→HTTPS 리디렉션 변경 후 실제 URL로 검증한다.
- 이미지에는 의미 있는 alt를 작성하되 장식 이미지는 빈 alt를 사용한다.

## 7. 실행·검증 명령어

저장소의 기존 `package.json`과 README를 먼저 확인하고 실제 명령어를 사용한다. 아래 명령은 존재할 때만 실행한다.

```bash
npm ci
npm run lint
npm run typecheck
npm run test
npm run build
```

명령이 없다고 임의의 스크립트를 만들지 않는다. 사용 가능한 검증 방법을 Pull Request에 기록한다.

## 8. 작업 완료 기준

- 요청 범위가 구현됐다.
- 관련 빌드·테스트·린트가 통과했다.
- 변경 전후 동작을 확인했다.
- 모바일과 데스크톱에서 깨짐이 없는지 확인했다.
- SEO 변경은 렌더링된 HTML 원문에서 확인했다.
- 새 환경변수가 있으면 `.env.example`과 배포 문서를 갱신했다.
- 변경 이유와 검증 결과를 Pull Request에 기록했다.

## 9. 중간 중단 및 인수인계

작업을 완료하지 못한 채 멈출 경우:

1. 안전한 중간 상태를 커밋한다.
2. 원격 작업 브랜치에 Push한다.
3. Draft Pull Request 또는 Issue에 다음을 기록한다.
   - 브랜치
   - 최신 커밋
   - 완료한 내용
   - 검증한 내용
   - 남은 작업
   - 막힌 이유
   - 다음 실행 명령
4. WIP 상태를 `main`에 병합하지 않는다.

## 10. 커밋 규칙

예시:

```text
feat(seo): add MedicalClinic structured data
fix(seo): correct canonical URL
chore: add deployment checklist
docs: record metadata decision
```

한 커밋에는 가능한 한 하나의 논리적 변경만 포함한다.

## 11. 배포 및 복구

- 작업 브랜치 또는 Pull Request에서 미리보기 배포를 먼저 확인한다.
- 검수 후 `main`에 병합한다.
- 운영 배포 후 실제 사이트를 다시 확인한다.
- 문제 발생 시 새 수정 커밋 또는 `git revert`를 사용한다.
- 운영 서버나 GitHub Pages 결과물을 직접 수정하지 않는다. 모든 변경은 소스 저장소로 역추적 가능해야 한다.

## 12. 최종 보고 형식

작업 종료 시 다음을 보고한다.

```markdown
- 작업 ID:
- 브랜치:
- 최신 커밋:
- 수정 파일:
- 변경 내용:
- 실행한 검증:
- 검증 결과:
- 배포 후 확인 필요 항목:
- 남은 문제:
```
